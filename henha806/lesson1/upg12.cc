// Default konstruktor - Inga värden
// Normal konstruktor - Skickar in värden
// Kopieringskonstruktor - Kopierar värden från en redan existerande instans av objektet. Genom in-parameter
// Delegerande konstruktor - Anropar en annan konstruktor för att utföra initialiseringen.

// Konstruktorer är viktiga för att säkerställa att ett objekt skapades på det tänkta viset.
// Det är viktigt att ha flera olika konstruktorer för att vara användarvänlig. Ger flexibilitet.